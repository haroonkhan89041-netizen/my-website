'use client';import{useState}from'react';import Link from'next/link';const data=['Build a multilingual AI support assistant','Rebrand a climate-tech startup for launch','Design a mobile banking experience','Make a sharp launch site for a seed-stage team'];export default function Marketplace(){const[q,setQ]=useState('');const rows=data.filter(x=>x.toLowerCase().includes(q.toLowerCase()));return <main><header className="nav"><div className="shell"><Link className="brand" href="/"><span className="mark"/>HUNAR</Link><Link className="btn accent" href="/signup">Join HUNAR</Link></div></header><section className="section"><div className="shell"><div className="eyebrow">Global project marketplace</div><h2>Find work with<br/>room to matter.</h2><input className="search" style={{margin:'25px 0'}} placeholder="Search projects, skills or companies" value={q} onChange={e=>setQ(e.target.value)}/><div className="grid">{rows.map(x=><article className="card" key={x}><div className="eyebrow">Fresh opportunity</div><h3>{x}</h3><p className="muted">Global client · 3 proposals</p><button className="btn light" style={{marginTop:20}}>View project</button></article>)}</div>{!rows.length&&<div className="empty">No projects in that orbit.</div>}</div></section></main>}
