'use client';import Link from'next/link';import{useState}from'react';import{supabase}from'../../lib/supabase';export default function Login(){const[e,setE]=useState(''),[p,setP]=useState(''),[m,setM]=useState(''),[show,setShow]=useState(false);async function go(x:React.FormEvent){x.preventDefault();const{error}=await supabase().auth.signInWithPassword({email:e,password:p});setM(error?.message||'Signed in.')}return <main className="section"><div style={{maxWidth:470,margin:'auto'}}><Link className="brand" href="/"><span className="mark"/>HUNAR</Link><section className="panel" style={{marginTop:40}}><div className="eyebrow">Welcome back</div><h2>Work globally.<br/>Start here.</h2><form className="form" onSubmit={go}><label>Email<input required type="email" value={e} onChange={x=>setE(x.target.value)} placeholder="you@example.com"/></label><label>Password<div style={{display:'flex',gap:8}}><input style={{flex:1}} required type={show?'text':'password'} value={p} onChange={x=>setP(x.target.value)} placeholder="Your password"/><button type="button" className="btn light" onClick={()=>setShow(!show)}>{show?'Hide':'Show'}</button></div></label><button className="btn accent">Sign in</button>{m&&<div className="notice">{m}</div>}</form><p className="muted"><Link href="/forgot-password">Forgot password?</Link> · <Link href="/signup">Create account</Link></p></section></div></main>}